# BathHack2026

Start the database:

```
docker run -d --name bussin-postgres -p 54320:5432 -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=bussin docker.io/postgis/postgis
```
